- name: List exported routes (sanity)
  run: |
    echo "---- out/ ----"
    if [ -d out ]; then
      find out -maxdepth 2 -type f -name "index.html" | sed 's#^out##' | sort
    else
      echo "::error ::No 'out/' directory was produced by the build."
      exit 1
    fi
